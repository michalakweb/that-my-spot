import React from "react"
import settingsIcon from "../../images/settingsIcon.png"

const LangSettings = () => (
	<div id="lang_settings">
		<img src={settingsIcon} id="settings_icon" />
	</div>
)

export default LangSettings
